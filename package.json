{
  "name": "sekiro-generator",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html -d docs --open --no-source-maps",
    "build": "parcel build index.html -d docs --no-source-maps"
  },
  "devDependencies": {
    "parcel": "^1.12.4",
    "typescript": "^4.0.5"
  }
}
